# Self Frame Example

This example represents the spec sketch as a Frame package.

It is intentionally self-referential:

- the spec describes what a Frame should look like
- this example uses that shape to describe the spec itself

The goal is to make the draft concrete enough to inspect and critique.
