# Rationale

This example exists to force the protocol sketch to describe a real Frame rather than remain only abstract.

Why this is useful:

- it makes the protocol easier to critique
- it exposes missing metadata or awkward field choices
- it shows how provenance references might work
- it clarifies the boundary between Frame semantics and Nebi packaging
