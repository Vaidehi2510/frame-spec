# Spec

This directory is reserved for more formal protocol artifacts once the current draft stabilizes.

Likely future contents:

- a human-readable spec
- a machine-readable schema
- validation examples

The current seed intentionally stops short of committing to a final schema because the protocol still needs more real examples.
